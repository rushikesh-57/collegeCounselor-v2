import Shell from '@/components/next/Shell';

export default function DownloadsPage() {
  return (
    <Shell>
      <section className="card">
        <h1>Downloads</h1>
        <p>Cutoff PDFs can be linked here as static URLs for indexable content.</p>
      </section>
    </Shell>
  );
}
