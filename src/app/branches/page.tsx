import Shell from '@/components/next/Shell';

export default function BranchesPage() {
  return (
    <Shell>
      <section className="card">
        <h1>Branches</h1>
        <p>Branch and career guide content can be rendered as static markdown-backed sections.</p>
      </section>
    </Shell>
  );
}
